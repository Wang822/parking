<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.backend.shop.dao.ChatDAO">
    <!--insert new chat-->
    <insert id="addNewChat" parameterType="Chat">
        insert into chat
        values (#{sender_id}, #{receiver_id}, #{time})
    </insert>
</mapper>
